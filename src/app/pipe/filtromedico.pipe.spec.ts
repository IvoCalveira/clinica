import { FiltromedicoPipe } from './filtromedico.pipe';

describe('FiltromedicoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltromedicoPipe();
    expect(pipe).toBeTruthy();
  });
});
