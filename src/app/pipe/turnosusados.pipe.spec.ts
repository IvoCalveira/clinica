import { TurnosusadosPipe } from './turnosusados.pipe';

describe('TurnosusadosPipe', () => {
  it('create an instance', () => {
    const pipe = new TurnosusadosPipe();
    expect(pipe).toBeTruthy();
  });
});
